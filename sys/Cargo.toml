[package]
name = "libarchive_sys"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
libc = "0.2.137"

[build-dependencies]
vcpkg = "0.2.15"
bindgen = "0.63.0"

[package.metadata.vcpkg]
dependencies = ["libarchive"]
